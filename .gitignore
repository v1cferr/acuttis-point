# Secrets — never commit real credentials
.env
.env.*
!.env.example

# Gleam
build/
*.beam
*.ez
erl_crash.dump

# Node
node_modules/

# Nix
result
result-*
.direnv/

# Runtime artifacts
logs/
screenshots/

# The saved browser session stands in for the password until it expires
*session*.json

# Estado local: sessão e screenshots (dado pessoal)
state/

# Internal FAI correspondence: local reference only, never published
*.eml
